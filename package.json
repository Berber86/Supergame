{
  "name": "dragon-raids",
  "private": true,
  "version": "0.1.0",
  "description": "Драконьи рейды: Королевства завоеваний — idle-кликер про дракона (статический сайт)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
