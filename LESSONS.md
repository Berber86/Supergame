# 🎓 LESSONS — накопленный опыт

Грабли, паттерны, советы себе будущему. Пополняется каждую сессию, новое — сверху.

---

## Сессия 10 (2026-07-30) · MLP

- ⚠️ **«Дорога в один конец» — худший класс дистбаланса**: тепло 5 сессий
  только расходовалось (рефиллов НОЛЬ). Юнит-тесты были зелёными — они
  проверяли механику, а не выживаемость. Правило: после связки «стат Х только
  тратится» обязан быть вопрос «а кто его ВОЗВРАЩАЕТ?» — до MLP, не в нём.
  Ловится только ЦИКЛОМ игры (бот-прогон на реальных редьюсерах, 60 жизней
  за 300 мс — проще не придумаешь).
- ✅ **Симулятор-политика как инструмент баланс-паса**: ~80 строк честного
  бота (копать/продать/есть/спать) выдали цифры для решения — и 5 регрессионных
  якорей в tests/balance.test.js навсегда. Не коммитить сам симулятор в /tmp —
  коммитить его сущность как тест.
- ✅ **Баланс-курс: лечить механику, не печатать деньги**: соблазн был «+20 ₽
  за бутылку». Вместо этого — еда и сон стали источниками тепла: игра
  получила ГОРЯЧЕЕ КАК РЕСУРС (новую текстуру выбора «поесть или замёрзнуть»),
  а не инфляцию. Фикс через смысл > фикс через щедрость.
- ⚠️ **Тест, прибитый к баланс-числу, падает при каждом тюнинге**: events-тест
  читал сумму «90 🔥» из устной традиции (80+10−1.6×mult). Усиление правила:
  ожидания баланс-зависимых величин считаются ИЗ ДАННЫХ (DECAY.warmthPerHour),
  не из калькулятора форума.
- ✅ MLP-гейт замеряется ботом, а не ощущением: «жизнь 10–30 минут» =
  медиана ~25 дней × ~40 действий/день ≈ реальные минуты. Числа в SESSION_LOG.

## Сессия 9 (2026-07-30)

- ⚠️ **Песочница может переклонироваться между сессиями**: сегодня локальная
  ветка внезапно стояла на main, а вся работа лежала «untracked» поверх.
  Паниковать нельзя: удалённая ветка — источник истины. Протокол на будущее:
  git status → если untracked на системных файлах — сверить `git log HEAD` с
  `git ls-remote` → `git fetch + reset --hard FETCH_HEAD` (потерь ноль, если
  последняя сессия запушена — а она ВСЕГДА запушена: правило «зелёный push
  в конце сессии» спасает).
- ✅ **Точки применения эффектов — комментарием в data**: слот снаряжения
  декларирует ЧИСЛО (riskMult 0.75), а шапка файла перечисляет, в какой строке
  ядра оно читается. Изменение эффекта не требует археологии.
- ✅ **«Надел — навсегда» решило снятия проблему на корню**: не нужен
  un-equip путь, возврат в переполненную ношу, перепродажа одёжек. Одна
  строчка геймдизайна иногда дешевле двадцати строк движка — записывай в GDD.
- ✅ Мёртвый навык (💪 xp не начислялся 5 сессий) реанимированобеесплатно по
  существующим паттернам: +1 за копку/переход — level уже осмыслен (+5 кг и
  +5 ⚡). Сначала сделай существующее живым, потом думай о новых фичах
  (ответ человека 2C это подтвердил).
- ⚠️ **edit_file иногда отвечает «Failed to read workspace file»** — ретрай
  проходит. Не считать за потерю данных, проверить цель после правки.

## Сессия 8 (2026-07-30)

- ✅ **Модалка как часть state** (`pendingEvent {eventId}`) — а не побочный
  DOM-эффект: модальное событие переживает сейв/F5 бесплатно (round-trip из
  коробки), UI лишь подписывается на него в refreshUi. «Состояние — единый
  plain-объект» снова окупилось.
- ✅ **choiceAvailability общая для серых кнопок и гварда редьюсера**: UI не
  даст кликнуть недоступное, редьюсер подстрахован, расхождений быть не может.
  Та же паттерн-идея, что saleOffer в сессии 7 («один источник для вида и для
  движка») — теперь это ДВА подтверждённых архетипа (цена / доступность).
- ✅ **Словарь эффектов данных → интерпретатор ядра** (events.js): баланс-
  дизайнер (я в будущем) пишет события декларативно, не трогая порядок
  применения. Порядок зафиксирован в шапке файла: деньги→статы→вещи→риск→
  флаги→спад модалки→часы. Новые ключи = сознательное расширение договора API.
- ⚠️ **Погодный множитель распада тепла — третья модификация «у меня в уме»**:
  после уроков сессии 5 (распад) и 7 (часы действия) поймался на warmthMult
  в тесте супа. Правило усилено: в ассертах статов за окно с часами —
  ВСЕГДА считать дельту действия + decay с модификаторами, не «красивые
  круглые ожидания».
- ⚠️ **Тест «ел дорогое» упал на гварде бедности** (60 ₽ против 200 ₽ пира) —
  напоминание: дорогие ветки селимся money = 500 до действия, а не после.
- ✅ Новые поля в сейвах снова без смены SAVE_VERSION: «добавочные» поля +
  подчистка значений (pendingEvent-привидение после ренейма данных; вчерашняя
  расписанка). Дешевле миграций, тестируется прямо через deserialize.

## Сессия 7 (2026-07-30)

- ✅ **Одна чистая функция цены для UI и для движка** (`saleOffer`): кнопка и
  редьюсер читают один источник — интерфейс физически не может соврать о цене.
  Рандом — только там, где это жанр (🕶️), и там UI честно показывает вилку+шансы.
  Паттерн «offer-функция» применять для будущих сделок/выборов с ценой.
- ✅ **Единая точка правды для повторяющейся формулы-риска**: честность Тени
  жила в identify.js и понадобилась в trade.js — вынес в `perekupHonestChance`,
  старый файл импортирует. Дуализм формул убит на этапе появления, а не после
  бага «почему вслепую и опознанное кидают по-разному».
- ✅ **Цена = центр собственной оценки игрока** («кот в мешке» 🧺): самообман
  стал механикой — промах глазомера теперь осязаем в ₽, а 👁️-петля замкнулась
  на экономику без единой новой шкалы. Красиво и дёшево в реализации.
- ⚠️ **Пассивный распад бьёт и во время сделок**: продажа на барахолке 4 ч —
  это −0.8⚡/ч и −0.6🧼 СВЕРХ стоимости действия. Тест с «ожидал 85⚡» упал на
  81.8. Правило (уже было для еды/сна — повторил ошибку): в тестах ожидания
  статов = дельта действия + распад за часы действия. Проверять обе части.
- ⚠️ «Срезовые» тесты (`sellableEntries`) считают по-бухгалтерски: сначала
  руками расписать кто что берёт, потом писать числа — моя «4 вместо 3» у пульпера
  показывает, что арифметика в уме и здесь ошибается.

## Сессия 6 (2026-07-30)

- ✅ **«Тайная цена при находке» + «estimate при оценке» — третий слой RNG
  вовремя** показал, зачем нужен роллер ПОСЛЕ advanceHours: тик времени может
  уйти в сон с роллами, и весь порядок бросков едет. Правило из сессии 5
  («роллер только после времени») теперь задокументировано как архитип:
  стоимость → время → роллер → эффект → xp → журнал → запись rngState.
- ✅ Золотая середина гонорара-прцента: игрок минимум знает (пол 100 ₽),
  сюрприз платит сполна — получилось честно и держит напряжение лотереи ❓.
- ⚠️ **includes() по текстам событий — хрупко**: регистр («Скандал» с заглавной)
  спрятал кидок от теста на 500 сидах. Правило: в assert'ах — проверять эффект
  (деньги/статы/записи), текст — только смоуком, и лучше ASCII-маркеры (✨ 🥷).
- ⚠️ Двойное округление при проверке множителя (round∘round) даёт дрейф 1 ₽:
  в стат-проверках множителей — допуск, а не точное равенство.
- ✅ Смоук-прогон «один сид — ноль ❓» напомнил: фичи с редким лутом
  прогонять по ПУЛУ сидов, пока нужное не выпадет (как тесты и делают).


## Сессия 5.5 (2026-07-30)

- ✅ **Самый массовый UX-грех самодельного SPA**: полный rerender на каждое
  действие = сброс скролла. Проявляется, когда контента стало больше одного
  экрана, — надо было ловить сразу, не ждать «UX ад» от человека. Лечение —
  4 строки (scrollY → rAF). Правило: любой rerender по действию обязан
  сохранять скролл по умолчанию, сброс — только явный.
- ✅ **Анатомия «визуального ада» одна и та же**: (1) одна инфа повторена N
  раз (8 баков × цена обыска), (2) навсегда развёрнутый флейвор,
  (3) карточка на каждый чих. Противоядия: инфу — ОДИН раз в шапку секции;
  флейвор — в нативный `<details>` (без строчки JS!); чих — в `.row`-строку
  «иконка · имя · суть · действие».
- ✅ Минимализм на живом продукте — это УДАЛЕНИЕ: −270 строк шаблонов,
  +0 логики, 0 новых тестов (98 зелёных без изменений) — а продукт лучше.
- ⚠️ Сам поставил себе ловушку: тест «SESSION_NUMBER — целое» честно взорвался
  на полусессии 5.5. Тесты на метаданные писать шире сразу.


## Сессия 5 (2026-07-29)

- ✅ **Симуляция «до кода» спасла фичу**: прикинул распад сытости (70 − 40/день)
  и понял, что без еды (сессия 8!) игрок умирает за 3–4 дня в любом раскладе.
  Досрочный минимум (съедобные находки) вошёл в сессию органично. Правило:
  новая механика → сначала день игры на калькуляторе, потом редьюсер.
- ✅ **Статистические тесты по выборке сидов** (обеднение: 150 сидов,
  среднее 1-го обыска ≫ 4-го; риск: 400 сидов ≈8%±4) ловят «сломал формулу,
  не заметил» надёжнее любых красивых точечных ассертов. А если нужен
  конкретный случай (отравление, полный пакет) — ищем сид циклом, не мокаем.
- ✅ **Нормализация вместо миграции версий**: старые сейвы (сессия 4) получили
  поля `bins`/`digMode` одним `normalizeState` в deserialize — человек ничего
  не потерял, SAVE_VERSION не тронут. Пока изменения только добавляют поля —
  так и держать.
- ✅ **Ленивая суточная запись баков** ({ day, digs[] } + сброс при смене дня)
  оказалась проще крюка «на новый день»: работает и после сна, и после
  полуночи посреди обыска, и после новой жизни. Проверка «day !== state.day»
  — весь механизм.
- ⚠️ **getBinsRecord мутирует на чтении** (ленивое создание записи). Для UI
  это удобно, но в тестах легко ошибиться ожиданием (я ждал null на
  валидном районе — получил свежую запись). Правило: читай докстринг,
  который сам же и написал.
- ⚠️ **UI-тексты, показывающие сырые id** (ночная кража: `${stolen.itemId}`
  вместо имени) — бесследны для unit-тестов и бросаются в глаза человеку.
  Регулярно grep-ать `$\\{[^}]*\\.itemId`/`\\.id}` внутри шаблонных строк событий.


## Сессия 4 (2026-07-29)

- ✅ **Тест на краевой случай полуночи поймал off-by-one до человека**:
  «проспал два часа вместо одного». Правило: каждый скачок времени
  (полночь, сон, новый день) — отдельным точным тестом с ожидаемым часом.
- ✅ **Детерминизм >= всяческие «примерно» проверки**: смерть в мороз
  воспроизводится фиксированным сидом, а проверка вероятности на выборке
  400 сидов (ожидаем 20% ± 6) стоит копейки и ловит кривую математику рук.
- ✅ Модель редьюсеров «мутируем state + пушим строки в events[]» сделала
  UI тонким без жертв: тосты, журнал и тесты едят один и тот же массив строк.
- ⚠️ Циклы импортов в UI (session↔tabs, session→death): работают, если все
  вызовы в рантайме; но для чистоты колбэки передавать параметрами
  (death.js получает startNewLife аргументом, а не импортирует session).
- ⚠️ Снова поймал свой мусорный импорт при перечтении (`advanceHoursTemplateNote`).
  Правило «написал файл — прочти его целиком» продолжает окупаться каждую сессию.

## Сессия 3 (2026-07-29)

- ✅ **Тест на демо-данные поймал реальный дефект до человека**: «ноша ≤ ёмкость»
  свалился на 10.7 кг при лимите 5 кг. Правило на будущее: любой демо-слепок
  состояния должен проходить те же инварианты, что и игровые данные.
- ✅ Кнопки-плейсхолдеры активными + тост «оживёт в сессии N» — лучше, чем
  disabled: человек видит будущую карту фич прямо в UI.
- ⚠️ **Self-review диффа перед коммитом — обязателен**: вёрстка без «глаз»
  приучилась к разумным дефолтам (auto-fit minmax, max-width), но в city.js
  протащил мусорный тернарник `ACTIONS ? 1 : 1` — поймал своим же перечтением.
  Читаем дифф целиком, даже когда «всё просто».
- ⚠️ Импорты DOM-модулей в тестах: держать `document.*` только внутри функций,
  не на верхнем уровне модуля — тогда node/vitest спокойно их импортирует и
  можно тестировать состав экранов без jsdom.
- ✅ Плагин-связка «данные → карточки»: все экраны рисуются из js/data, поэтому
  смена баланса автоматически меняет UI-текст (цены, теги, риски).

## Сессия 2 (2026-07-29)

- ✅ **Первое действие сессии — `git fetch` + `gh pr view`**: убедиться, что
  изменилось со стороны человека (PR слит? CI добавлен?), прежде чем строить
  планы. Дёшево, спасает от работы по устаревшей картине.
- ✅ **Данные-черновики держать строго декларативными**: схема в комментарии
  вверху файла, ноль функций. Тогда валидация тестами сводится к
  «соответствует ли схеме и согласованы ли таблицы» — и ловит 90% ошибок
  контента ещё до появления логики.
- ✅ Тесты на сумму шансов (`toBeCloseTo(1)`) и на существование всех
  `itemId` в лут-таблицах — мгновенно окупаются при правках контента.
- ⚠️ Не доверять счётчикам из головы: количество предметов в сводках
  пересчитывать скриптом (`node -e`), один раз уже поправлял «39» → «38».
- ⚠️ Версия живёт в двух местах: `js/core/config.js` и `package.json` —
  поднимать обе (`npm version x.y.z --no-git-tag-version`).

## Сессия 1 (2026-07-29)

**Что сработало / что держать в уме**

- ✅ Жёсткое правило среды: работать только в ветке `arena/019faf3c-supergame`,
  пушить только её; в `main` код попадает через PR. Это расходится с
  «работай напрямую в main» из README — правило среды важнее, расхождение
  писать в SESSION_LOG, чтобы не удивлять себя в будущих сессиях.
- ⚠️ **Токен агента не умеет пушить workflow-файлы** (нужен permission
  `workflows`). Не тратить время на повторные попытки: изменения в
  `.github/workflows/` передавать человеку через CHECKLIST (вставка через
  веб-интерфейс) или просить выдать приложению право. GitHub даёт похожую
  ошибку и через REST API contents — проверено, обхода нет.
- ✅ `npm ci` в CI требует `package-lock.json` в репозитории — генерировать
  `npm install` локально и коммитить lock при каждой смене package.json.
- ✅ Сайт без сборки: Vercel по умолчанию отдаёт статику из корня —
  `vercel.json` не создавать без реальной нужды (меньше конфигов — меньше грабель).
- ✅ Репозиторий приватный и маленький: документы памяти живут в корне рядом
  с кодом — так их точно не потеряешь между сессиями.
- ⚠️ Эмодзи как «спрайты»: до первого визуала у человека проверять базовый
  набор иконок через `CHECKLIST.md` — разные ОС рисуют их по-разному.
- ⚠️ Не плодить игровую логику до сессии 4: README это запрещает, а ранняя
  логика до финализации GDD — выброшенная работа.
