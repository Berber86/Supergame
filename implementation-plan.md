# План реализации — «Одиссей: Путь теней» (после анализа 1.5.0)

Выбранные направления (решения от 2026-08-01) и разбиение на два этапа реализации.

---

## Этап I — Рок и честный интерфейс ✅ РЕАЛИЗОВАН (версия 1.6.0)

Самодостаточные изменения с низким риском: механика, которая уже была в данных, получает эффект; интерфейс перестаёт скрывать смертельную информацию.

### I.1. «Рок» становится механикой (проблема 3 — вариант «Дать Року механику»)
- [x] Рок ≥40: штормы чаще (+3 п.п. к шансу) — `doomStormModifier` применён в `continueVoyage`, `travelPreview`, `scoutNextRoute`.
- [x] Рок ≥60: стражи сильнее (+15% к урону намерений), ≥80 — +25% — `doomBossMultiplier` в `resolveBossAction`.
- [x] Рок ≥80: роковые знамения в пути (45% на переход): −8 духа, −2 воды, −1 гребца + текст в журнале.
- [x] Рок ≥50: божественный финал закрыт — подношение Аиду (−5 рока) и прозрение Идмона (−10) обретают смысл как страховка перед финишем.
- [x] Экран «Судьба» показывает пороги рока; бейдж командной панели загорается с рока ≥40.
- [x] Тесты: `doom as a real mechanic` (5 кейсов) в `src/game.test.ts`.

### I.2. Мобильный интерфейс и честность смерти (проблема 5 — вариант «Полная строка + подсветка смерти»)
- [x] Мобильная строка ресурсов: все 6 ресурсов + драхмы (сетка 7 колонок; драхмы выделены цветом).
- [x] Сравнение курсов: подсветка «lethal» для переходов, где кончатся пища/вода/дух или корпус уйдёт в ноль при шторме; список причин («Гибель: кончатся вода, пища»).
- [x] Предупреждение «Рок высок: в пути возможны роковые знамения» при роке ≥80 перед отплытием.
- [x] Экран гибели: «Последний счёт» — причина (экспортированный `deathReason`), остатки всех ресурсов (обнулённые подсвечены), совет по ресурсу-убийце, последняя запись журнала.

### I.3. Честная подача маршрута (проблема 4 — вариант «Не исправлять», сопутствующая копия)
- [x] README: «процедурный мир из 187 локаций» → «процедурно рисуемая карта и авторский маршрут из двенадцати узлов».
- [x] Версия пакета 1.5.0 → 1.6.0, раздел журнала изменений в README.

---

## Этап II — Структура режимов и подготовка-пакеты ✅ РЕАЛИЗОВАН (версия 1.7.0)

### II.1. Режимы = разная структура (проблема 1 — вариант «Режимы = разная структура»)
- [x] `DifficultyDefinition` получил структурные параметры: `voyageIslands` (7 | 10), `crewCostMultiplier`, `divineInterference`.
- [x] «Сказание» — короткий путь: 7 островов (12 узлов), мягкие проверки, 1 спасение Афины.
- [x] «Одиссея» — полный путь: 10 островов (15 узлов, порты на 3 и 8, стражи на 6 и 13), канонические проверки.
- [x] «Гнев богов» — тот же полный путь, но другие правила: наём гребцов ×2 (потеря людей почти необратима), боги вмешиваются в переходы (12% на переход: −5 духа, −1 вода).
- [x] Генератор маршрута (`createRoute`) параметризован сложностью; `actForNode` учитывает длину маршрута.
- [x] Перебалансировка до целей симуляции (оптимальный бот, 200 сидов): Сказание 67.5%, Одиссея 31%, Гнев богов 14.5%; застреваний нет.
- [x] Штормы честнее: базовый риск 0.36 → 0.30, урон 5–13 → 4–11; намерения стражей (трезубец −18 → −14, щупальца −15 → −12, течение −9 → −7) и провальная ретилиация (×1.22 → ×1.12) смягчены.
- [x] Симулятор (`simulation.ts`) стал «оптимальным капитаном»: покупает услуги по разумным порогам, улучшения, снаряжение и спутников; выбирает пакет по состоянию корабля.
- [x] `simulation.test.ts` проверяет целевые полосы проходимости (Сказание ≥50%, Одиссея ≥25%, Гнев ≥8%) и иерархию Сказание > Одиссея > Гнев.
- [x] Описания и теги режимов в меню переписаны под структуру (длина пути, правила найма и гнева богов).

### II.2. Подготовка — пакеты (проблема 2 — вариант «Свернуть в пакеты»)
- [x] Одно решение «Как готовимся к переходу?» с тремя пакетами вместо 4–6 микро-кликов:
  - «Осторожно» — путь на день дольше, +5% к проверкам, отдых при отплытии (+20 здоровья, +6 духа за 3 пищи и 3 воды);
  - «Стандартно» — без изменений;
  - «Спешно» — путь на день короче, −5% к проверкам, +2 духа.
- [x] Пакеты автоматически включают отдых/тренировку/разведку: отдельные кнопки отдыха, тренировки и разведки удалены (`restHero`, `trainSkill`, `scoutNextRoute` больше не существуют).
- [x] `PreparationState` упрощён: `travelMode` вместо `preparedSkill`/`restedNodeIndexes`/`trainedNodeIndexes`/`scoutReport`; вахта, пайки, подношения и назначение спутника остались.
- [x] Выбор пакета: карточка исхода, порт (баннер отплытия + сравнение), вкладка «Судно»; сравнение всех трёх пакетов с честными цифрами и подсветкой гибели.
- [x] Калибровка: бот с пакетами (осторожно при здоровье <55 или духе <50, иначе спешно) даёт целевые проходимости из II.1.
- [x] Сохранения 1.6.0 мигрируются (ключ v11): старый поход сохраняет маршрут, подготовка получает `travelMode: 'standard'`.
- [x] Тесты: `preparation.test.ts` переписан под пакеты (предпросмотр, отдых/скорость, проверки), `game.test.ts` и `companionSagas.test.ts` обновлены, e2e-порт обновлён («Отплыть» + пакеты).

**Проверки Этапа II:** 104 теста (13 файлов) зелёные, `eslint` чист, `npm run build` и `npm run test:base` проходят; симуляция на 200 сидах: Сказание 67.5% / Одиссея 31% / Гнев богов 14.5%, ноль застреваний; иерархия режимов зафиксирована тестами.

---

## Итоговое состояние

Обе задачи этапов выполнены и закоммичены в рабочую ветку. Дальнейшие направления (не входили в согласованный объём): ветвящийся маршрут (проблема 4, вариант 4.1), «побочные высадки» (4.2), прозрачные цели концовок (3.3), вводный пролог и глоссарий (5.3).

---

## Этап A — Один авторский голос и чистка лексики ✅ РЕАЛИЗОВАН (версия 1.8.0)

### A.1. Единые имена и один голос (проблема 5 — вариант «Один авторский голос»)
- [x] `currentEncounter`: заголовок события = имя острова (карта, кодекс и экран сходятся); главы спутников сохраняют название песни.
- [x] `resolveChoice`: убрана склейка немедленного исхода с общим литературным слоем (`outcome.text` — текст выбора, последствия — авторские из `islands.ts`).
- [x] Тест «speaks with one authorial voice» в `game.test.ts` (20 сидов × все узлы маршрута).

### A.2. Чистка лексики (проблема 2 — вариант «Чистка лексики»)
- [x] «ложный манифест» → «поддельная грамота» (`game.ts`, `App.tsx`, README);
- [x] «гештальт» → «старый счёт» (`companionSagaEncounters.ts`);
- [x] «расход»/«расходный материал» → «плата» (`companionLiteraryLayer.ts`, `companionSagaIslands.ts`);
- [x] «документальный долг перед системой» → «письменный долг в счётных книгах»; «действовать стратегически» → «править дальнейшим путём»; «ресурс/ресурсы» в прозе → «припасы» (`islands.ts`, `game.ts`).

**Проверки Этапа A:** 105 тестов (13 файлов) зелёные, `eslint` чист, `npm run build` и `test:base` проходят.

---

## Этап B — Шесть прологов и голоса спутников ✅ РЕАЛИЗОВАН (версия 1.9.0)

### B.1. Шесть прологов (проблема 1 — вариант «Сюжетные якоря актов»; идея пользователя: пролог по числу комбинаций пары стартовых спутников)
- [x] Новый файл `src/prologue.ts`: 6 прологов «Пепел Трои» — по числу сочетаний C(4,2) из двух стартовых спутников (eurylochus+tiphys, eurylochus+sinon, eurylochus+idmon, tiphys+sinon, tiphys+idmon, sinon+idmon); у каждой пары свой заголовок, сцена и голоса.
- [x] Пролог показывает состав портретами и ролями и завершается первым решением без броска: клятва (+дух, +рок), паруса до рассвета (−дух, −рок), взгляд на пожар (+дух, +рок).
- [x] `RunState.prologuePending` (схема v12): новые походы начинаются с пролога, старые сохранения мигрируют без повторного проигрывания; `resolvePrologue` применяет эффекты и пишет запись в журнал.
- [x] Панель `ProloguePanel` с новой панорамой `public/art/prologue-troy.jpg`; e2e-тесты обновлены (пролог перед первой встречей).
- [x] Тесты: покрытие всех шести пар, разрешение пролога, миграция.

### B.2. Персонализация голосов спутников (проблема 3 — вариант «Персонализировать голоса»)
- [x] Переписаны arrival/hook/aftermath всех пяти состояний × четырёх спутников (60 текстов) с уникальной лексикой: Еврилох — военный/горький (строй, приказ, счёт живых), Синон — театральный/ироничный (маска, сцена, зрители), Тифий — морской/деловой (румб, киль, трюмная вода), Идмон — пророческий/отстранённый (нить, тень, свет). choiceTitle/Description и числа отношений сохранены.

### B.3. Знамения пути и голоса с палубы ✅ РЕАЛИЗОВАНО (версия 1.10.0)
- [x] Глашатай Посейдона в акте 2 — сцена-знамение (`heraldPending`, `poseidonHeraldScene`): смириться / ответить вызовом / промолчать; меняет богов и рок, без броска.
- [x] Сцена «Порог Итаки» перед финалом (`thresholdPending` + `thresholdDone`, `ithacaThresholdScene`): текст помнит число спасённых, последнее решение и рок; последний выбор — царём / Никем / человеком.
- [x] Концовки получили вступление от якорей (`endingIntro`): спасённые, стражи, рок, последнее решение.
- [x] Реплики по составу команды для авторского пула (`rosterSafeText`): имена отсутствующих спутников заменяются постоянными гребцами (Еврибат/Перимед/Филий) в crewVoice/aftermath/consequence.
- [x] Принцип пула зафиксирован: тексты — только для 35 авторских островов (23 общих + 12 саговых); процедурные локации карты без текстов.
- [x] Схема сохранений v13; симулятор разрешает обе сцены; e2e-ключ обновлён.
