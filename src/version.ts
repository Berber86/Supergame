/**
 * Версия игры — тихая подпись на стартовой странице.
 *
 * Берётся прямо из `package.json`: сборщик оставляет в бандле одну строку,
 * и в разработке, и в сборке значение одинаковое. Отдельного `define`
 * в конфиге не нужно — он работал бы только в собранной версии.
 */

import { version } from '../package.json';

export const APP_VERSION = version;
