# Статус проекта

- Сессия 3 (Coder) завершена: скелет проекта (Vite + TypeScript + Vitest), CI, `ROLE_STATE.md`, веха «Инфраструктура».
- Игра: открывается статический сайт с рабочим кликом по золоту, пассивным доходом и сейвом в localStorage (каркас; полные петли — в сессиях 4+).
- Как запустить: `npm install`, затем `npm run dev` (или `npm run build && npm run preview`). Тесты: `npm test`.
- Автодеплой на Vercel — шаг для человека (импорт репозитория в панели Vercel), см. `CHECKLIST.md`.
- Следующий шаг — сессия 4 (Coder): полная idle-петля (найм слуг, здания, сейв/лоад UI).
